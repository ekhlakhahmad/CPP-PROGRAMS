// Write a program to Multiply of two number.

#include <iostream>
using namespace std;
int main()
{
    int a, b, m;
    cout << "Enter any two number: ";
    cin >> a >> b;
    m = a * b;

    cout << "Multiply of " << a << " and " << b << " = " << m;
    return 0;
}